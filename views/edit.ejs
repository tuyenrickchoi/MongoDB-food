<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Food</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Food Management</h1>
    </header>
    <div class="container">
        <h1>Edit Food</h1>
        <form action="/foods/<%= food._id %>?_method=PUT" method="POST">
            <!-- Name Field -->
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= food.name %>" required>
            
            <!-- Price Field -->
            <label for="price">Price:</label>
            <input type="number" id="price" name="price" value="<%= food.price %>" required>
            
            <!-- Image URL Field -->
            <label for="img">Image URL:</label>
            <input type="text" id="img" name="img" value="<%= food.img %>">
            
            <!-- Address Field -->
            <label for="address">Address:</label>
            <input type="text" id="address" name="address" value="<%= food.address %>">
            
            <!-- Category ID Field -->
            <label for="category_id">Category ID:</label>
            <input type="text" id="category_id" name="category_id" value="<%= food.category_id %>">
            
            <!-- Submit Button -->
            <button type="submit">Update Food</button>
        </form>
        <a href="/foods" class="back-link">Back to Food List</a>
    </div>
</body>
</html>
